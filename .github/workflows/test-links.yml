name: Check Links
on: push

jobs:
  Check-Website-Links:
    runs-on: ubuntu-latest

    steps:
      - uses: CannonLock/test_links@master
        with:
          file_ignore: "/./staff/.*/,/./includes/.*/,/./doc/.*/"